import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//ReactDOM method to render the app component in the DOM.
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);